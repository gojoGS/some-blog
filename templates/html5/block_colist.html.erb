<div class="colist <%= attr 'style' %>">
  <ul>
    <% items.each_with_index do |item, i| %>
      <li>
        <span class="conum" data-value="<%= i + 1 %>"><%= i + 1 %></span>
        <div class="text"><%= item.text %></div>
      </li>
    <% end %>
  </ul>
</div>